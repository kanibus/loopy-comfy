# ============================================================================
# LOOPY COMFY REQUIREMENTS
# ============================================================================
# 
# PYTHON VERSION COMPATIBILITY (Updated 2024):
# ✅ Python 3.12.x (RECOMMENDED - Active development, best performance)
# ✅ Python 3.11.x (Supported - Security fixes only until 2027)
# ✅ Python 3.10.x (Supported - Good compatibility) 
# ❌ Python 3.13+ (NOT SUPPORTED - Missing wheel distributions)
# ❌ Python 3.9 and below (NOT SUPPORTED - Missing features)
#
# WHY PYTHON 3.12?
# - Official binary installers available (unlike 3.11 security-only mode)
# - 5-10% better performance than 3.11
# - All scientific packages now have 3.12 wheel distributions
# - Active bug fixes and feature updates
#
# INSTALLATION TROUBLESHOOTING:
# If you encounter errors, try these commands in order:
# 1. pip install setuptools wheel
# 2. pip install -r requirements.txt
# 3. For Windows: Use Visual Studio Build Tools if compilation fails
# 4. For macOS: Install Xcode Command Line Tools
# 5. For Linux: Install build-essential and python3-dev
#
# COMFYUI EMBEDDED PYTHON FIX (if nodes fail to load):
# cd /path/to/ComfyUI_windows_portable
# ./python_embeded/python.exe -m pip install "numpy<2" opencv-python==4.9.0.80 --force-reinstall
# See OPENCV_FIX_NOTES.md for detailed troubleshooting guide
#
# ALTERNATIVE INSTALLATION (CONDA - RECOMMENDED FOR PROBLEMATIC SYSTEMS):
# conda create -n loopy-comfy python=3.12
# conda activate loopy-comfy
# conda install numpy opencv scipy scikit-learn
# pip install ffmpeg-python imageio imageio-ffmpeg pytest pytest-cov
# ============================================================================

# Build System Requirements (Install FIRST if you have issues)
setuptools>=65.0.0,<70.0.0
wheel>=0.40.0,<1.0.0
pip>=23.0.0

# Core Numerical Computing
numpy>=1.24.0,<2.0.0           # Mathematical operations and arrays
scipy>=1.10.0,<2.0.0           # Scientific computing functions

# Computer Vision and Image Processing  
opencv-python>=4.8.0,<5.0.0    # Video and image processing (4.9.0.80 tested for ComfyUI compatibility)
Pillow>=10.0.0,<11.0.0         # Image manipulation and formats

# Video Processing and Encoding
ffmpeg-python>=0.2.0,<1.0.0    # FFmpeg wrapper for video encoding
imageio>=2.30.0,<3.0.0         # Image and video I/O operations
imageio-ffmpeg>=0.4.8,<1.0.0   # FFmpeg backend for imageio

# Machine Learning (Optional - for future features)
scikit-learn>=1.3.0,<2.0.0     # Machine learning algorithms

# Development and Testing Tools (Optional)
pytest>=7.4.0,<8.0.0           # Testing framework
pytest-cov>=4.1.0,<5.0.0       # Coverage reporting

# Platform-specific dependencies (automatically handled by pip)
# Windows: Microsoft Visual C++ 14.0+ (auto-installed with setuptools)
# macOS: Xcode Command Line Tools (install with: xcode-select --install)  
# Linux: build-essential python3-dev (install with package manager)